name: userlocation
description: 1st Semester Finals
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.4.0
  flutter_polyline_points: ^2.0.0
  google_maps_flutter: ^2.5.1
  geocoding: ^2.1.1
  geolocator: ^10.1.0
  http: ^1.1.0
  uuid: ^3.0.7
  custom_info_window: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  - images/1.png
  - images/2.png
  - images/3.png

